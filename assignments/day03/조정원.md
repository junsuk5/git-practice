# [TIL] Day 03 - Java 문법 기초 (워밍업)

- 작성자: 조정원
- 작성일: 260916

---

## 1. 오늘 내가 직접 손으로 치며 배운 점
- UpDownGame에서 매개변수는 target과 userGuess 두가지야. 
- boolean을 while의 조건 자리에 써서 판정 결과를 예/아니오 둘로 말할 수 있어.
- 시작할때 isCorrect=false는 게임을 시작하지 않았는데 맞췄을 리가 없기 때문이야.
- guess의 숫자가 isCorrect가 되면 isCorrect = checkGess(targetNumber, guess)로 넘어가서 총 몇번 만에 정답을 맞추었다고 프린트 하고 끝나는거 아님?
-public class UpDownGame {

    static final int MIN = 1;
    static final int MAX = 100;   // ★ 이제 여기 한 줄만 고치면 끝

    public static void printWelcome() {
        System.out.println("================================");
        System.out.println("🎮 " + MIN + "부터 " + MAX + " 사이의 숫자를 맞춰보세요!");
        System.out.println("================================");
    }
    ...
    int targetNumber = new java.util.Random().nextInt(MAX) + MIN;
-min과 max를 정하면 1~100까지 숫자를 정할수 있음. 코드는 클라우드 코드가. 
-static final int MAX_TRIES = 5;   // 아까 배운 상수로

...

while (!isCorrect) {
    System.out.print("숫자를 입력하세요: ");
    int guess = scanner.nextInt();
    attempts++;

    isCorrect = checkGuess(targetNumber, guess);

    // ★ 여기
    if (!isCorrect && attempts >= MAX_TRIES) {
        System.out.println("💀 기회 소진! 정답은 " + targetNumber + "였습니다.");
        break;
    }
}
-attempts의 숫자를 제한하면 됌. 코드는 클라우드 코드가.

## 2. 가장 멘붕이었던 순간 & 트러블슈팅
- 깃헙에 올리고 나서 타이틀이랑 본문쓰는거 있잖아. 헷갈렸음.
- 물어봐서 씀.

## 3. 나만의 언어로 재해석한 핵심 용어 사전
- 새로운 용어를 배우고 싶음. 
